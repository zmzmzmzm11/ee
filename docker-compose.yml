# EveBox + Elasticsearch Docker Compose
# 前提: Suricata 已在宿主机运行，eve.json 输出到 ./suricata-logs/
# 启动: docker compose up -d --build
# 停止: docker compose down

services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:7.17.28
    container_name: elasticsearch
    network_mode: "host"
    environment:
      - discovery.type=single-node
      - "ES_JAVA_OPTS=-Xms1g -Xmx1g"
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - esdata:/usr/share/elasticsearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://127.0.0.1:9200/_cluster/health | grep -qE 'green|yellow'"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s
    restart: unless-stopped

  evebox:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        GIT_REV: ${GIT_REV:-unknown}
        BUILD_REV: ${BUILD_REV:-unknown}
    image: evebox:latest
    container_name: evebox
    network_mode: "host"
    volumes:
      - ./suricata-logs:/var/log/suricata:ro
      - evebox-data:/var/lib/evebox
    command:
      - --datastore
      - elasticsearch
      - -e
      - http://127.0.0.1:9200
      - --input
      - /var/log/suricata/eve.json
      - --port
      - "5636"
    depends_on:
      elasticsearch:
        condition: service_healthy
    restart: unless-stopped

volumes:
  esdata:
    driver: local
  evebox-data:
    driver: local
